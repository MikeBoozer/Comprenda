# Screenshots — README "See it run" gallery

Drop four PNGs here, captured from the **live Comprenda app**. Source of truth for
what each shows: `go_to_market/demo_script.md` → "Portfolio stills to grab". The
filenames below are referenced directly by `README.md` and by the LinkedIn kit
(`go_to_market/linkedin_portfolio_post.md`), so **the names must match exactly**.

| Filename | Capture | Source page |
|---|---|---|
| `01_overview.png` | Editorial UI + KPI row (**Events 8 · Languages 12 · Posts 1,440**) | Workbench → Overview |
| `02_plcs_risk_score.png` | Scored market cards + the 0–100 risk spectrum (your hero / thumbnail) | Workbench → Pre-launch risk |
| `03_divergence_matrix.png` | The 12×12 heatmap + the selected-pair aside | Analysis → Divergence matrix |
| `04_ai_brief.png` | Brief header + first section + a figure | Synthesis → AI brief |

**Capture tips:** light theme, 100% zoom, a consistent window width (the content
column caps at 1280px — give it room). Export PNG; keep each under ~500 KB if you can
(employers browse on phones). Crop out browser chrome where it doesn't add anything.
