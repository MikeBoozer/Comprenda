# Dependencies for the PUBLIC DEMO (streamlit/demo_app.py) on Streamlit Community
# Cloud / Hugging Face Spaces. Deliberately light: the demo runs on fixture data
# and stubs Snowflake, so no snowflake-snowpark-python is needed.
#
# (The in-Snowflake app uses streamlit/environment.yml instead — that's the SiS
# package set and is unrelated to this demo deploy.)
# Upper cap at <1.59: the app still uses the deprecated `use_container_width=`
# param (Streamlit slates its removal after 2025-12-31). 1.58.x is verified-good
# via a headless smoke test of all 10 views (snowpark blocked); capping here stops
# a future minor from dropping the param and breaking the live demo. Lift the cap
# once use_container_width is migrated to `width=`.
streamlit>=1.40,<1.59
pandas>=2.0
altair>=5.0
